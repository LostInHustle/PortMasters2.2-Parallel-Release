{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "NODE_ENV=development npm install --include=dev && ./node_modules/.bin/prisma generate"
  },
  "deploy": {
    "startCommand": "export NODE_ENV=development NEXT_TELEMETRY_DISABLED=1 DATABASE_URL=\"${DATABASE_URL:-file:../db/custom.db}\" && ./node_modules/.bin/prisma db push --skip-generate && ./node_modules/.bin/tsx server.ts",
    "healthcheckPath": "/api/health",
    "requiredMountPath": "/app/db",
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "drainingSeconds": 20
  }
}
